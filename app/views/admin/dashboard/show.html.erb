<h2> Orders </h2>

<p><h3> Packaged </h3></p>

<section id = "packaged">
<% @orders.each do |order| %>
  <section id = "packaged-order<%= order.id %>">
    <% if order.status == "packaged" %>
      <p>Name: <%=link_to "#{order.name}", "/admin/profile/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Date Created: <%= order.created_at %></p>
      <p><%= button_to "Ship this Order", "/admin/orders/#{order.id}", method: :patch %></p>
    <% end %>
  </section>
<% end %>
</section>


<p><h3> Pending </h3></p>

<section id = "pending">
<% @orders.each do |order| %>
  <section id = "pending-order<%= order.id %>">
    <% if order.status == "pending" %>
      <p>Name: <%=link_to "#{order.name}", "/admin/profile/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Date Created: <%= order.created_at %></p>
    <% end %>
  </section>
<% end %>
</section>

<p><h3> Shipped </h3></p>

<section id = "shipped">
<% @orders.each do |order| %>
  <section id = "shipped-order<%= order.id %>">
    <% if order.status == "shipped" %>
      <p>Name: <%=link_to "#{order.name}", "/admin/profile/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Date Created: <%= order.created_at %></p>
    <% end %>
  </section>
<% end %>
</section>

<p><h3> Cancelled </h3></p>

<section id = "cancelled">
<% @orders.each do |order| %>
  <section id = "cancelled-order<%= order.id %>">
    <% if order.status == "cancelled" %>
      <p>Name: <%=link_to "#{order.name}", "/admin/profile/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Date Created: <%= order.created_at %></p>
    <% end %>
  </section>
<% end %>
</section>
